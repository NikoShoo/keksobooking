import { setMap } from "./map.js";

//Нужно заблокировать форму и фильтр - (написать модуль и вызвать команды).
setMap()
	.then(() => {
		// Размораживаем форму которая внизу
		// Начинаем получение данных - fetch
		// Если данные успешно получены - then
		// Наичнаем отрисовку пинов
		// Размораживаем фильтры
	})
	.catch();
